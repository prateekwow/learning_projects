 # for i in range(0,len(arr)-1):
    #     # break
    #     for j in range(i+1,len(arr)):
    #         if arr[i] > arr[j] :
    #             swapping(arr[i],arr[j])
    #             arr[i] = arr[i]+arr[j]
    #             arr[j] = arr[i]-arr[j]
    #             arr[i] = arr[i]-arr[j]
    # print(arr)    